apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana-dashboard-loader
  namespace: open-cluster-management
spec:
  ports:
  - name: http
    port: 8080
    targetPort: http
  selector:
    app: grafana-dashboard-loader